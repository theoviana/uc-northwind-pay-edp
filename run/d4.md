# Day 4 — follow-along (stub)

Staff execute file. Scope: [`agenda/d4.md`](../agenda/d4.md).
Papers: [`docs/`](../docs/README.md). Brain: **query** packs 04–07, do not rebuild.

**One Night.** Deck not built yet. Do not invent beats. When the Orchestrator
slides exist, write them here as a Day-1-shaped folder: Show then Hands-On.

**Not tonight (Night 3).** Type 01 Gold is [`d3/`](d3/README.md).

Story that night: SeamWise **again** (remaining swimlanes + legs) → Task-Spec
**generates remaining SWE + DE leaves** (`02`–`04`, Type `05`, orchestrate) →
**Linear** is the board → Mesh + Pass **6–8** crank the queue (cross-family if
doctor is green) → Type `05` unattended + small `HALF_UP` pill. Direct and
orchestrated must hash the same. Host: pin Task-Spec 3.8.x, `cvg init`, project
**this** plant’s seams (`ingest-landing`, `dlt-gold`, `orchestrate-serve`) — do
not copy foreign lane names. Type `06` stays sealed.

# 02 · Converge & Linear Registration

- Slide: Execute 02 (Hands-On **slice b · register** · chip `run/d5/` · **02**)
- Slice: **B · Register**
- Who: instructor copies in public, then seats
- Next: [`03-look-up-02-05.md`](03-look-up-02-05.md)

Linear is the **board**, not the judge. Thursday Linear MCP was down; [`docs/tasks/_linear-board.md`](../../docs/tasks/_linear-board.md) **is** the queue. Copy it. Do not recut the night because Linear.com is empty.

`cvg/` was inited Thursday. Seams already projected: `ingest-landing`, `dlt-gold`, `orchestrate-serve`. Pin Task-Spec **3.8.x** if `cvg` dies on 3.9 (`CVG_TASKSPEC_BIN`).

## Prompt (verbatim)

```text
Do not crank.
Do not author Type 06.
Do not copy assurance / foundation / models.

Register the Thursday queue.

1. Read docs/tasks/_linear-board.md. That file is the board if Linear.com is empty.
2. Confirm cvg/swimlanes/ is ingest-landing, dlt-gold, orchestrate-serve.
3. Split cards:
   - Settled: T-20260825-type-01-landing-parser (signed_off true, packet evidence/loop/T-20260825-type-01-landing-parser.json, eval exit 0)
   - Queued: every T-20260827-* plus remaining Type 01 emit/dlt/B/S/G/match — signed_off false. Do not move them.
4. If Linear is up, copy one card per leaf. Linear moves when a packet exists, not when chat says done.
5. If Linear is down, say so and keep _linear-board.md.

Do not stamp remaining leaves. Do not write modern/ packages.
```

## Proof

Settled vs queued named from disk. Seams are this plant’s names. No remaining-type crank.

## If fail

Foreign lane names → delete and re-project. Stamping unsigned 02–05 on this tile → stop; those stay dark until after Type 06 (or leftover time).

# Day 4 — Orchestrator

**Seat:** Orchestrator. The seat that **leaves**.  
**Rings:** **Loop + eval.** Bind still on.  
**Converge:** Recap. Teach Context · Eval · Loop. Then Pass **6–8** (Register / Bind / Loop). **Linear** is the board. Type `05` unattended last.  
**This file** is the brief for the Day 4 PPT. Hand it to a deck LLM. Fewer slides than Night 3. Deeper into [`docs/`](../docs/README.md) and [`plans/modern.md`](../plans/modern.md).  
**Deck:** `presentation/d4-orchestrator.html` — Stage live; Floor/Dig follow this clock. Identify by **`data-act-name`**. Staff: [`run/d4/`](../run/d4/README.md) — 9 beats.

Public gate: *the loop assumes; it validates and records itself.*  
**One Night.** No morning / afternoon.

---

## Story this week (read before the projector)

Tue–Wed were **Type 01 steel threads**. Thursday teaches the machine that can leave the keyboard, then **proves** it.

| Night | Seat | Lane | What we **perform** | What we **do not** |
|---|---|---|---|---|
| **2** Tue | Translator (SWE) | Seam 1 · ingest → landing | Sign ingest. Parser leaf. | Lakehouse. Loop. Linear. |
| **3** Wed | Constructor (DE + analytics) | Seam 2 · dlt → Gold | Type 01 landing → Gold + golden-match **when the worktree closed it** | Recut ingest Consensus. Types `02`–`05`. Pass 6–8. |
| **4** Thu | Orchestrator | Context · Eval · Loop, then remaining types | Teach three surfaces. Generate remaining SWE+DE. Crank. Type `05` unattended + `HALF_UP`. | Recut Tue/Wed signs. Type `06`. Mesh-inside-Task-Spec. |
| **5** Fri | Dark Factory | Sealed Type `06` | Full 0–8. Classify. Do not patch `legacy/`. | — |

Do not copy foreign lane names (`assurance` / `foundation` / `models`). This plant: **`ingest-landing`**, **`dlt-gold`**, **`orchestrate-serve`**. Recap Type 01 Gold **from disk**. Gap → do not dump `02`–`05`.

---

## Night 4 agenda (the projector)

Nights 2–3 **performed**. Tonight the room **understands** three surfaces, then the factory runs.

Public numbers are **capabilities**. Clock puts Bind (Craft) **before** generate so an unbound agent never dumps `02`–`05`. Identify slides by **`data-act-name`**.

- [ ] **End-to-End**
  1. *Recap* — Type 01 Gold **or named gap**. Keep 173.44. [`docs/`](../docs/README.md) is the paper trail. [`plans/modern.md`](../plans/modern.md) is the engagement contract.
  2. *Orchestrator Role* — the seat that **leaves**. If you are still at the keyboard, it is not orchestrated.
  3. *Context Engineering (Context Layer)* — enterprise query path: brain (mail) · OntoLayer (where paid lives) · `docs/` (signed) · `contracts/` (judge) · `evidence/` (observations). Ontology is **not** the plant. Agents that migrate or golden-match **ask here first**.
  4. *Eval Engineering* — one leaf, one runnable command, one fail. Green log ≠ green money. On this plant: `golden_match.py`, two questions, six codes, no tolerance.
  5. *Telemetry (Observability)* — the loop writes a **packet** the way `make run` writes `reconciliation.json`. No packet → no settle. Linear moves only what was recorded.
  6. *Agent Harness — Loop + Eval* — rings tonight. Bind still on. Unsigned leaf never cranks.
  7. *Loop Engineering (GOAL, bubble wrap, Task-Mesh)* — GOAL = signed Task-Spec. Wrap: Bind + eval red does not settle. Task-Mesh **runs** the leaf. It is **not** inside Task-Spec.
  8. *Converge (Passes 6, 7, 8) + Linear* — Register so the mesh sees the queue. Bind still on. Loop cranks. Linear is the board, not the judge. Then Type `05` unattended + `HALF_UP`.

Show, then Hands-On. Recap is **one act**. Research is Close, not a ninth public beat. Type `05` is the **look-up of beat 8**.

**Swing:** Recap → Orchestrator role → Context Layer → Eval → Telemetry → Harness (Loop+Eval) → Loop / Mesh → Converge 6–8 + Linear (generate, crank, Type `05`).

---

## HUD swing

Same mold as Days 1–3. Six blocks. **Fewer slides** (~28). Identify by **`data-act-name`**, not a HUD integer (extras shift the counter).

| Block | HUD | Mode | Agenda beats |
|---|---|---|---|
| **Opening** | 01 | title | Orchestrator |
| **Stage** | 02–11 | talk | 1 Recap · 2 Orchestrator Role · 3 Context Engineering · 4 Eval Engineering · 5 Telemetry · 6 Agent Harness Loop+Eval · 7 Loop Engineering · 8 Converge 6–8 + Linear |
| **Craft** | 12–14 | Show | Bind still on (rails, **not** a public beat). Kits. Host pin + `cvg init` |
| **Floor** | 15–17 | Show + Hands-On **A** | Trail — week as one picture (not a paid-grain quiz) |
| **Dig** | 18–24 | Show + Hands-On **B–D** | Remaining on two lanes · generate/queue · Type 01 crank · Type `05` |
| **Debrief** | 25–28 | close | In hand · Research · Next · silent Tomorrow |

Craft is the fence **before** they generate remaining types. Floor is the **trail**. Dig places remaining on the same two lanes, then cranks Type 01. Mesh stays **beside** Task-Spec. Type `06` is silent Tomorrow.

---

## PPT blocks (build in this order)

**Deck file:** `presentation/d4-orchestrator.html`  
**Chrome:** clone [`presentation/d3-constructor.html`](../presentation/d3-constructor.html) HUD, tokens, Hands-On tiles (`hs-head`, `hs-board`, look up / do not). Night 2 live reference: [`presentation/d2-translator.html`](../presentation/d2-translator.html). Do **not** invent a new visual system.

**~28 slides.** One idea per slide. Every block opens with a divider (except Opening). Do not go past 28. Identify slides by **`data-act-name`**.

Four Hands-On boards only (**A–D**). Leave · SeamWise parks, opens [`presentation/seamwise.html`](../presentation/seamwise.html), returns. Task-Spec Show parks [`presentation/task-spec.html`](../presentation/task-spec.html), returns. Do not paste generate or crank from inside a kit.

| Block | Slides | Count |
|---|---|---|
| Opening | 01 | 1 |
| Stage | 02–11 | 10 |
| Craft | 12–14 | 3 |
| Floor | 15–17 | 3 |
| Dig | 18–24 | 7 |
| Debrief | 25–28 | 4 |
| **Total** | **01–28** | **28** |

### Beats → slides (not 1:1)

| Beat | Slides | HUD |
|---|---|---|
| 1 Recap | 2 (Gold · papers). Tight, one act | 03–04 |
| 2 Orchestrator Role | 1 | 05 |
| 3 Context Engineering | 1 (ontology folded in) | 06 |
| 4 Eval Engineering | 1 (this plant’s referee on the same slide) | 07 |
| 5 Telemetry | 1 | 08 |
| 6 Agent Harness — Loop + Eval | 1 | 09 |
| 7 Loop Engineering | 1 Stage + Task-Mesh Show in Dig | 10 · 22 |
| 8 Converge 6–8 + Linear | 1 Stage + Hands-On **B–D** | 11 · 20 · 23–24 |
| Floor trail | 1 Show + Hands-On **A** | 16–17 |
| Research | 1 Close | 26 |

### Slide list

| HUD | Block | `data-act-name` | Kind |
|---|---|---|---|
| 01 | Opening | Orchestrator | title |
| 02 | Stage | Stage | divider |
| 03 | Stage | Recap · Gold | Recap. Type [`run/d4/01`](../run/d4/01-prompt-recap.md) if tables missed Wed (**no** HO badge) |
| 04 | Stage | Recap · papers | Recap · `docs/` + `plans/modern.md` |
| 05 | Stage | Orchestrator Role | story |
| 06 | Stage | Context Engineering | story · Context Layer. Ontology is not the plant |
| 07 | Stage | Eval Engineering | story · basics + `golden_match.py` |
| 08 | Stage | Telemetry | story · the packet |
| 09 | Stage | Agent Harness · Loop + Eval | story · rings tonight |
| 10 | Stage | Loop Engineering | story · GOAL, bubble wrap, Mesh beside Task-Spec |
| 11 | Stage | Converge 6–8 + Linear | story |
| 12 | Craft | Craft | divider |
| 13 | Craft | Bind is rails | Show |
| 14 | Craft | Kits tonight | Show. Host pin Task-Spec 3.8.x + `cvg init` |
| 15 | Floor | Floor | divider |
| 16 | Floor | Trail · show | Show · week as one picture |
| 17 | Floor | **Execute 02** | Hands-On **A** · trail |
| 18 | Dig | Dig | divider |
| 19 | Dig | Leave · SeamWise | Park. Remaining onto two lanes. Return |
| 20 | Dig | **Execute 03–04** | Hands-On **B** · place + generate/queue |
| 21 | Dig | Task-Spec | Show. Pause kit. Return. Mesh is **not** inside it |
| 22 | Dig | Task-Mesh · Linear | Show · packet on settle |
| 23 | Dig | **Execute 05–06** | Hands-On **C** · Type 01 crank + packet |
| 24 | Dig | **Execute 07–08** | Hands-On **D** · Type 05 + `HALF_UP` |
| 25 | Debrief | In hand | close |
| 26 | Debrief | Research | type [`run/d4/09`](../run/d4/09-research.md) |
| 27 | Debrief | Next | close |
| 28 | Debrief | Tomorrow | silent · Type `06` sealed |

### Hands-On boards (Day 1 / Day 2 / Day 3 mold)

Chip · `run/d4/` range · one tile per beat · look-up / do not. No slide per prompt. Staff: [`run/d4/README.md`](../run/d4/README.md).

| Board | Slice | `data-act-name` | `run/d4/` | Tiles | Look up when |
|---|---|---|---|---|---|
| **A** | trail | Execute 02 | [`02`](../run/d4/02-walk-trail.md) | 02 six chapters | requisites → problem → decisions → SWE → DE/AE → tonight · right/wrong started |
| **B** | lanes | Execute 03–04 | [`03`](../run/d4/03-place-lanes.md)–[`04`](../run/d4/04-generate-queue.md) | 03 place · 04 generate + queue | remaining on SWE vs DE vs tonight · leaves with evals · Linear shows the queue |
| **C** | loop | Execute 05–06 | [`05`](../run/d4/05-mesh-crank.md)–[`06`](../run/d4/06-packet.md) | 05 Type 01 crank · 06 packet | packet on disk · Type 01 hash **if Gold exists** |
| **D** | pill | Execute 07–08 | [`07`](../run/d4/07-type05-unattended.md)–[`08`](../run/d4/08-half-up.md) | 07 unattended · 08 `HALF_UP` | `DF-SOURCE-005` = `CONFIRMED_SOURCE_DEFECT` · `HALF_UP` · never rewrite `expected/` |

If Type 01 Gold is missing → **dark B–D**. If generate has no evals → **dark C–D**.

### What each talk slide must say (builder copy)

| `data-act-name` | Must include |
|---|---|
| Orchestrator | Seat: Orchestrator. Gate: the loop assumes; it validates and records itself. One Night. Fewer slides than Wednesday. |
| Recap · Gold | Type 01 vertical **or named gap**. Keep **173.44**. Do not recut Tue/Wed signs. `02`–`04` were parked, not tasked. |
| Recap · papers | [`docs/`](../docs/README.md) is papers. [`plans/modern.md`](../plans/modern.md) is the engagement contract. ADR 0006 rows **8–9** open tonight (Dagster, serve). |
| Orchestrator Role | The seat that **leaves**. If you are still at the keyboard, it is not orchestrated. |
| Context Engineering | Four rooms, one path: Second Brain (mail) · OntoLayer (where paid lives) · `docs/` (signed) · `contracts/` (judge). `evidence/` is observation. Ontology is **not** the plant — `catalog_ask` / `make ontology-ask` is read-only. Guessed joins are how Gold lies. Agents that migrate or golden-match **ask here first**. Paid = `reporting.card_settlement_reconciliation` (`batch_id` + `currency`). |
| Eval Engineering | One leaf, one runnable command, one fail. “The agent said OK” is not an eval. On this plant: [`validation/golden-match/golden_match.py`](../validation/golden-match/golden_match.py). Two questions, never netted. Six codes: `CONFIRMED_SOURCE_DEFECT` · `CONFIRMED_LEGACY_DEFECT` (Friday) · `MODERN_DEFECT` · `APPROVED_BEHAVIOR_CHANGE` · `CONTRACT_AMBIGUITY` · `UNRESOLVED`. No tolerance. Do not rewrite the referee. |
| Telemetry | The loop writes a **packet** the way `make run` writes `reconciliation.json`. Capture: attempt, leaf, eval, exit, classification, paths. No packet → no settle. Linear moves only what was recorded. Not a new vendor. Path: `evidence/` (gitignored — open in the **terminal**). |
| Agent Harness · Loop + Eval | Rings tonight = **Loop + eval**. Bind **still on**. Frozen: `legacy/` `contracts/` `gen/` `infra/`. Unsigned leaf never cranks. A prompt is not a fence. |
| Loop Engineering | GOAL = signed Task-Spec. Bubble wrap: Bind + unsigned dark + eval red does not settle. Step: register → claim one leaf → run → eval → **record packet** → retry/settle. Task-Mesh **runs** the leaf. It is **not** inside Task-Spec. |
| Converge 6–8 + Linear | **6 Register** — mesh sees the queue. Host: pin Task-Spec 3.8.x, `cvg init`, project **this** plant’s seams (`ingest-landing`, `dlt-gold`, `orchestrate-serve`). **7 Bind** still on. **8 Loop** cranks. Linear is the **board**, not the judge. Then Type `05` unattended (`DF-SOURCE-005`) and `rounding-half-up` = `HALF_UP`. |
| Bind is rails | Still on. Write surface grew (orchestrate + Type 05 paths). Frozen trees refuse writes. |
| Kits tonight | Converge 6–8. SeamWise **again** (remaining lanes). Task-Spec generate remaining SWE+DE. Task-Mesh runs + packet. Linear board. |
| Trail · show | Week as one picture. Requisites → problem → decisions → SWE → DE/AE → tonight. Not a paid-grain quiz. |
| Leave · SeamWise | Park. `presentation/seamwise.html`. Remaining **swimlanes + legs** (`02`–`04` ingest and lakehouse, Type `05`, orchestrate). Return. Do not recut Type 01. Do not write seams from that HUD. |
| Task-Spec | Generate remaining leaves. One leaf, one eval. `signed_off` false. Empty packages forbidden. Type 05 evals include `DF-SOURCE-005` and `rounding-half-up`. Mesh is the **next** Show. |
| Task-Mesh · Linear | How a signed leaf runs. Emits the packet. Linear moves on settle. Not the author of the leaf. |
| In hand | A loop that **records**. Green run with no packet is a failed settle. `HALF_EVEN` default is `MODERN_DEFECT`. |
| Research | (1) factory / flywheel, cite a page (2) do **not** search Type `06` (3) restate two lies; what might the third be? |
| Next | Friday Dark Factory. Sealed Type `06`. Classify, do not patch `legacy/`. |
| Tomorrow | Silent. Type `06` sealed. |

### Creation moves (one block per pass)

1. **Chrome** — copy Day 3 HUD, tokens, Hands-On tile. Empty deck shell. HUD `01`/`28`. Title Orchestrator.
2. **Opening + Stage (01–11)** — eight public beats as story only. Review before any Hands-On.
3. **Craft (12–14)** — Bind Show + kits. Host pin named. No new fail-closed board.
4. **Floor (15–17)** — Context Layer Show + board **A**.
5. **Dig (18–24)** — Leave · SeamWise + board **B** + Task-Spec Show + Mesh Show + boards **C–D**.
6. **Debrief (25–28)** — In hand, Research, Next, silent Tomorrow.

Do not start Dig until 01–11 read as one story. Do not add a fifth Hands-On for Mesh. Do not reprint medallion, DE 101, or engines (Wednesday). Do not author Type `06` slides as execute.

### Builder constraints (do not invent)

- Clone Day 3 chrome. Do not invent a new visual system.
- Identify slides by **`data-act-name`**, not HUD integers.
- `java2py` is a nickname from Tuesday — do not revive it as tonight’s engine.
- Do not recut Day 2 ingest Consensus or Day 3 lakehouse sign.
- Do not write `modern/` product code on a Show slide.
- Do not put Task-Mesh **inside** Task-Spec.
- Do not open Type `06`.
- Do not repair `173.44`.
- Do not add a new observability vendor. The packet is `evidence/`.
- Do not copy foreign swimlane names.
- Staff execute folder: [`run/d4/`](../run/d4/README.md) — 9 beats, four Hands-On boards + Close. Trail first.
- Method kits: [`presentation/seamwise.html`](../presentation/seamwise.html), [`presentation/task-spec.html`](../presentation/task-spec.html). Converge manual: [`presentation/cvg-aut-systems-spine-steps.html`](../presentation/cvg-aut-systems-spine-steps.html).
- Papers: [`docs/`](../docs/README.md). Engagement contract: [`plans/modern.md`](../plans/modern.md) Milestones 4–5.
- Brain: [`brain/notebooklm/`](../brain/notebooklm/README.md) — query, nine packs. Pack 07 = Type 05.
- Referee: [`validation/golden-match/golden_match.py`](../validation/golden-match/golden_match.py) — do not rewrite for slack.
- Ontology: [`ontology/README.md`](../ontology/README.md) — `catalog_ask` / `make ontology-ask`. Read-only.

### Prompt for the deck LLM (paste with this file)

```text
Build presentation/d4-orchestrator.html from agenda/d4.md.

Clone chrome, HUD, tokens, and Hands-On tile mold from presentation/d3-constructor.html.
~28 slides. Identify every slide by data-act-name from the Slide list.
Public beats are the eight End-to-End items. Research is Close, not a ninth lecture.
Four Hands-On boards only: Execute 02, Execute 03–04, Execute 05–06, Execute 07–08.
Tile labels must match run/d4/ beat numbers.
If agenda/d4.md and run/d4/ disagree on Hands-On ranges, run/d4/ wins on the clock.
Do not invent extra story, a fifth Hands-On, Mesh-inside-Task-Spec, Type 06 execute, or a new visual system.
```

---

## Intent

They leave able to:

1. Query a **Context Layer** before migrating or classifying (not grep SQL as the judge).
2. Write an eval that can fail — on this plant, golden-match + a leaf command.
3. Require a **packet** before settle.
4. Run Converge 6–8 with Linear as the board.
5. Classify Type `05`: `DF-SOURCE-005` = `CONFIRMED_SOURCE_DEFECT`; `rounding-half-up` is `HALF_UP`; `HALF_EVEN` = `MODERN_DEFECT`. Do not change `expected/`. Do not break Java.

They do not open Type `06`. They do not recut Tue/Wed signs. They do not put Mesh inside Task-Spec.

---

## Craft — kits

| Kit | Job tonight |
|---|---|
| **Converge** | Pass 6–8. Coordinator writes no product code. `cvg init` if no workspace. |
| **SeamWise** | **Second** pass: remaining type lanes + orchestrate/serve. |
| **Task-Spec** | Generate remaining SWE + DE. Type `05` evals include `rounding-half-up` and `DF-SOURCE-005`. |
| **Task-Mesh** | Runs signed leaves. Emits the packet. **Not** inside Task-Spec. |
| **Linear** | Board. Projection of signed leaves. Not the judge. |

---

## Floor / Dig (live)

Staff clock: [`run/d4/`](../run/d4/README.md).

1. Query Context Layer (`docs/`, OntoLayer, `plans/modern.md` Milestones 4–5).
2. Host: pin Task-Spec 3.8.x. `cvg init`. Project **this** plant’s seams.
3. SeamWise remaining swimlanes + legs. Task-Spec generate. Linear: the queue is visible.
4. Crank **one** signed leaf first. Look up the packet under `evidence/` (terminal).
5. Type 01 under Dagster only if Gold exists — same hash as direct. Parsing stays in the plant.
6. Bind Type `05`. Leave the table. Then open `rounding-half-up`.

---

## Research

Same notebook. No tenth pack. Type `06` is not in it.

1. **Second Brain** — “What is a factory, and what is the flywheel?” Cite a page.
2. Do **not** search the repo for Type `06`.
3. Restate the two lies you already met. What might the third be?

---

## Exit criteria

- [ ] Context Layer restated from `docs/` + OntoLayer + `plans/modern.md`.
- [ ] Remaining SWE + DE leaves authored with evals (`02`–`04`, Type `05`, orchestrate). Empty packages deleted.
- [ ] Linear shows the queue. A cranked leaf left a **packet**.
- [ ] Type `05` unattended: `DF-SOURCE-005` = `CONFIRMED_SOURCE_DEFECT`.
- [ ] `rounding-half-up` is `HALF_UP`. `HALF_EVEN` classified `MODERN_DEFECT` and fixed in the plant, not `expected/`.
- [ ] Frozen `legacy/` untouched. Type `06` absent.

## Day 4 is not

- Not Day 3 (no medallion reprint; no recut of lakehouse sign).
- Not Day 5 (no sealed kit, no `CONFIRMED_LEGACY_DEFECT` hunt on `01`–`05`).
- Not Mesh-inside-Task-Spec.
- Not a new observability vendor. The packet is `evidence/`.
- Not CI, cloud, or Terraform.

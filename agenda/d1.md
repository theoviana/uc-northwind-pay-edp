# Day 1 — Archaeologist

**Seat:** Solutions Architect & AI Engineer (the Archaeologist).  
**Converge:** Pass **0 Capture** and Pass **1 Intent**. Human-led. **No product code.**  
**This file** is the brief for the Day 1 PPT, the follow-along setup, Second Brain, OntoLayer, and the tech-spec.  
Keep it short on stage. Build slides from the chapter list. Do not invent extra story.

**Deck:** [`presentation/d1-archaeologist.html`](../presentation/d1-archaeologist.html) (44 slides).  
**Staff:** [`run/d1/`](../run/d1/README.md) — six slices A–F, then Close 17.  
**Brain:** [`brain/notebooklm/`](../brain/notebooklm/README.md) — nine packs, whole drop, queried all week.

**Swing:** Stage (the plant / the one cent / brownfield) → Craft (seat from scratch, Slice A) → Floor (boot the plant, Slice B) → Dig (read, brain, graph, spine, Slices C–F) → Debrief (Research, then tomorrow).

Public gate (bootcamp page): *the system understands the legacy better than you.*  
Rings tonight: **prompt + context.** HITL total. You stay on the keys.  
**One Night.** There is no morning and no afternoon.

---

## Intent

Everyone arrives brownfield. Nobody in the room built this plant. They behave as an **AI-native engineer from scratch**: they do not inherit a blessed IDE, a pre-wired brain, or last run's ADRs. They leave able to:

1. Say what NorthWind Pay is, why a one-cent miss matters, and draw both plants (first write is different).
2. Name the tree: `spec/` is inbound, `contracts/` is the judge, `legacy/` is frozen, `modern/` does not exist yet.
3. Set up the **agentic development environment** on their laptop and sit any agent that can edit files and run shell.
4. Boot the repo with the instructor — one Type `01` batch **MATCHED**, net `173.45` — so the plant is a fact, not a slide.
5. Stand up the **Second Brain** and feed it the drop: mail, meetings, policies, inbound for types `01`–`05`. It is the memory the rest of the week reads. Raw samples stay on disk.
6. Stand up **OntoLayer** on the live legacy Postgres and ask it questions **via MCP**. Show the same question **without** (`make ontology-ask-sql`) and **with** (`make ontology-ask`).
7. Run Converge **0 Capture** (BRD from the estate, or kill it cheaply) and **1 Intent** (tech-spec that answers the brief. No premature stack).

They do not write ADRs (Pass 2 is tomorrow). They do not cut seams. They do not sign Consensus. They do not write `modern/`. They do not open Type `06`. They do not "fix" a trailer.

---

## Stage — chapters for the PPT

Use this order. One idea per slide. The room does not know the domain yet, so the business comes first. Follow along: do not dump the whole tree in one breath.

### 1. Money arrives as files, once a night

- NorthWind Pay settles card payments for merchants. Money does not arrive as an API call.
- A batch lands overnight on SFTP. Java 21 parses it, Postgres holds it, the merchant is paid.
- Five live file types (`01`–`05`). Type `01` — card settlement — is the one we live in tonight.
- Distilled from a real scar (Kurv), not a toy shop.

### 2. One file, opened

- Not JSON. Not CSV. **Fixed-width mainframe records** at byte offsets fixed by the contract.
- `spec/type-01-card-settlement/samples/valid-minimal.dat` — one `H`, two `D`, one `T`.
- Amounts are **signed overpunch**. `1234E` = `123.45`. Trailer `1734E` = `173.45`. Rows agree. Batch accepted.

### 3. The same file, one letter different

- Trailer `E` → `D`. `173.45` becomes `173.44`. One byte carries the money.
- **Keep the lie. Refuse the batch.** The refusal is a result, not a crash.

### 4. Green is not the goal

- Parse can PASS. Type-check can PASS. Money: **−0.01**.
- **No oracle, no build.** Inbound prose does not outrank `contracts/`.

### 5. Two plants, same bytes (draw this)

Legacy (already on the machine after boot):

```text
SFTP raw/incoming → Java 21 → SFTP csv/outgoing
  → COPY staging.* → procedures → reporting.* → oracle
```

Modern (Days 2–4 — not on the tree yet):

```text
same SFTP raw/incoming → Python → modern/landing/ Parquet
  → dlt (register only) → DuckLake / DuckDB
  → dbt Bronze → Silver → Gold → golden-match
```

### 6. The week (so tonight has a map)

| Day | Seat | Rings | Converge | Autonomy |
|---|---|---|---|---|
| 1 | Archaeologist | Prompt + context | **0–1** | HITL total — you understand |
| 2 | Translator | Harness | **2–4**, then 5–8 on ingest → landing | HITL strong — you validate |
| 3 | Constructor | Harness + loop seed | Recap. **2–4–5** on dlt → Gold | HITL receding — Type 01 Gold |
| 4 | Orchestrator | Loop + eval | Context · Eval · Loop. Then **6–8**. Type `05` unattended | Loop — the factory records and corrects |
| 5 | Dark Factory | Orchestration | Full 0–8 on sealed Type `06` | You leave the loop |

Type `06` is **not** in `spec/` until Friday morning.

### 7. Context is the product tonight

- A repo tour is not context. A **Second Brain** that is fed the drop is.
- Schema guessed by a chat is not context. **OntoLayer** over live Postgres, asked via MCP, is.
- Converge 0–1 compiles that context into a brief and a tech-spec. Structure (ADRs) waits until the brain and the graph exist.

---

## Craft — AI-native seat from scratch

People will show up with Claude Code, Codex, Kimi, Cursor, Oh My Pi, OpenCode, or nothing but a terminal. **The curriculum is the repo and the gates.** The agent is a seat. Tonight we install the seat together.

House stack: Oh My Pi → OpenRouter → **a workspace** (CMUX, ORCA, Super Engineering, or BYO) → DeepSeek. We grade gates, not the vendor.

### Machine (required)

- Git
- Docker with Compose
- GNU Make
- Python 3.12 or newer

If these four work, every later day works. If they do not, stop. Do not debug an agent.

### Repo (Slice B — after the plant story, not before boot)

```bash
git clone <this-repo>
git checkout main
make init
make deploy
make status
make run TYPE=01 SCENARIO=valid-minimal
```

`05` sits in Slice B on purpose. Do not run 06–09 against a machine that never `make deploy`.  
Done when evidence shows `B202607230000001` **succeeded** and reconciliation **MATCHED**, net `173.45`. `evidence/` is gitignored — open it in the **terminal**.

### Agent (from scratch)

Day 1 does **not** install one blessed IDE.

| Rule | Why |
|---|---|
| Any agent that can edit files and run shell can sit here | We grade gates, not vendors |
| The agent reads `README.md` first | Same operator surface for everyone |
| Then `spec/estate/`, then `spec/type-01-card-settlement/` | That is the drop |
| May read `legacy/`, `contracts/`, `plans/` | Brownfield grounding |
| Must not write `legacy/`, `contracts/`, `gen/`, `infra/` | Frozen plant |
| Must not create `modern/` | The barrier has not been signed |
| Bind is tomorrow's fence | Tonight the proof is "do not change any file" |

**Slice A — the seat** (Hands-On Execute 01–04). Do not `make status` yet. Boot is Slice B.

Staff: [`run/d1/01-oh-my-pi.md`](../run/d1/01-oh-my-pi.md)–[`04-machine-four.md`](../run/d1/04-machine-four.md).

### Second Brain

The living memory of the project. It is fed **inbound**, not invented:

- `spec/estate/cover.md`, mail, meetings, policies
- Per-type inbound for **all five live types** (`01`–`05`) — layouts, procs, walk-throughs
- The lie in prose (same shape on every type). Raw `.dat` samples stay on disk
- Whatever else the room drops (no frozen `legacy/` source as "truth")

It must be queryable all week. A pile of Markdown in a random folder is not a Second Brain.

### OntoLayer

Crawl the **live** local Postgres (schema + procedures) → enrich → serve via **MCP**.

The Dig shows the same business question twice:

1. **Without** the graph — staff `make ontology-ask-sql`; agent may read `legacy/postgres`. Guessed joins stand.
2. **With** the graph — staff `make ontology-ask` or MCP `catalog_ask`. Grain, keys, and "what paid means" are retrieved, not hallucinated.

That contrast is the value. Do not skip the "without" pass. Do not ask the graph what Converge is.

### Converge tonight

Show the spine first, then run 0–1 (same pattern as Second Brain and OntoLayer). `cvg` gates; the agent drafts.

```text
0 Capture → BRD from the estate via the Second Brain (or kill it cheaply)
1 Intent  → tech-spec answers the brief. No premature stack.
```

Pass 2 Structure (ADRs), Pass 3 Decompose, Pass 4 Consensus wait until **Day 2**. An unsigned tech-spec is not a license to code tonight.

---

## Floor — Slice B · plant

Instructor drives, room types. **Boot first.** Progressive. Do not jump.

1. **Boot.** `clone` · `make init` · `make deploy` · ping `boot-ok`. Staff: [`run/d1/05-boot.md`](../run/d1/05-boot.md).
2. **README, then the plant.** Agent reads `README.md` (no writes). `make status`. `make run TYPE=01 SCENARIO=valid-minimal`. Open `evidence/B202607230000001/reconciliation.json` in the **terminal** — MATCHED, net `173.45`. Staff: [`06`](../run/d1/06-prompt-readme.md)–[`09`](../run/d1/09-receipt-matched.md). Deck: Hands-On Execute 05–09.

Do not walk Type `05` rounding. Do not show `modern/`. Do not “fix” 173.44.

## Dig — Slices C–F · read, brain, graph, spine

Show, then the matching Hands-On. Verbatim prompts live in `run/d1/`. Do not flip a slide per prompt.

3. **Tree, not a tour.** Point at `spec/` (inbound), `contracts/` (judge), `legacy/` `gen/` `infra/` (frozen), `evidence/` (the run), `modern/` (not here). Open `spec/estate/cover.md` as mail. Staff: [`run/d1/10-tree.md`](../run/d1/10-tree.md).
4. **Ask the seat.** Three prompts, same pair, no writes. SA: four truth roles + what is frozen. AI engineer: per-folder read / write / neither. Then evidence. Do not create `modern/`. Staff: [`run/d1/11-prompt-estate.md`](../run/d1/11-prompt-estate.md). Deck: Hands-On Execute 10–11.
5. **Second Brain.** Show first. Each person opens an **empty** NotebookLM, unzips [`brain/notebooklm/northwind-pay-brain.zip`](../brain/notebooklm/northwind-pay-brain.zip), uploads the **nine** `.md` files (the zip itself is not a source), asks the seven questions. The pack is the whole drop (estate + inbound for types `01`–`05`). Type `01` is tonight’s steel thread; Days 2–5 keep asking this notebook. Prove one retrieval: Marina’s 173.44 vs 173.45. `spec/` stays on disk for Capture. Staff: [`run/d1/12-notebooklm.md`](../run/d1/12-notebooklm.md).
6. **OntoLayer.** Show first. Same question twice — without via `make ontology-ask-sql`, then with MCP / `make ontology-ask`. Write the difference on the board. Staff: [`run/d1/13-ontolayer.md`](../run/d1/13-ontolayer.md).
7. **Converge 0–1.** Show the spine first. Name the kits from the repo, then thin install. Pass 0 Capture: BRD from what the brain actually holds (or kill it cheaply). Pass 1 Intent: tech-spec that answers the brief. No stack, no ADRs, no seams. Staff: [`14`](../run/d1/14-converge.md)–[`16`](../run/d1/16-intent.md). Deck: Show · Converge, then Hands-On Execute 14–16.

Do not run Pass 2–4. Do not ask the Postgres graph what a kit is.

## Debrief — Close 17, then walk

8. **Research.** Query the brain, the graph, and the repo for Day 2. Citations, not a re-read. Staff: [`run/d1/17-research.md`](../run/d1/17-research.md). Deck: Research, then Next, silent Tomorrow.
9. **Stop.** Point at tomorrow: Structure → Decompose → Consensus, then the first modern write.

---

## What you will hold in your hand at the end

- A local runtime that is healthy, and one Type `01` batch that MATCHED.
- An agentic seat that can read the repo without rewriting Java.
- A **Second Brain** — nine packs, types `01`–`05`, queryable all week. Type `06` is not in it.
- **OntoLayer** over live Postgres, reachable via MCP, with a recorded without/with contrast.
- A Capture brief + an Intent tech-spec the room can restate.
- Three Research citations for Day 2: a page, a routine, the seams.
- No ADRs required. No seams. No Consensus. No `modern/` on disk.

---

## Debrief

- The plant already runs. You did not migrate anything tonight.
- Context is the product: brain + graph beat a clever prompt.
- Converge 0–1 compiled that context. Structure is tomorrow. Consensus is still the barrier — it is **not** tonight.
- The source can lie. The inbound can lie. **The contract is the judge.**
- Role skills named the seat. Deliverables named what you hold. Research named what you query. Then you walk.

---

## Homework

1. Runtime still healthy (`make status`). Do not create `modern/`.
2. Confirm the Second Brain answers one question without re-reading the files by hand.
3. Confirm OntoLayer still answers via MCP after a restart.
4. Re-read the tech-spec. Restate it without the file open.
5. Bring the same agent tomorrow. It will be bound, not trusted.
6. Do not read Type `06` — it is not there.

---

## Research

Close the night with queries, not a re-read. Ask the tools for what Day 2
will need to answer:

1. **Second Brain** — "What does *paid* mean for Type 01, and which meeting
   contradicts the layout?" The answer must cite a page.
2. **OntoLayer** — "Which procedure writes
   `reporting.card_settlement_reconciliation`, and what is its grain?"
3. **Repo** — name the seams raw → sanitize → stage → apply → report, and
   who owns each arrow.

---

## Exit criteria

- [ ] Local runtime healthy; Type `01` valid-minimal **MATCHED**, net `173.45`.
- [ ] Can draw both plants and name the first write on each side.
- [ ] Can separate inbound, contract, observation, and Git without mixing them.
- [ ] Agentic seat works: README + `make status`, no frozen writes.
- [ ] Second Brain is live and fed from `spec/estate/` plus inbound for types `01`–`05`. Type `06` is not in it.
- [ ] OntoLayer indexed live Postgres; one question asked via MCP; without/with contrast recorded.
- [ ] Pass 0 Capture artifact exists. Pass 1 Intent tech-spec exists.
- [ ] Research citations exist: a page, a routine, the seams.
- [ ] No ADRs presented as signed. No seams cut. No Consensus. No `modern/`. No Type `06`.

## Day 1 is not

- Not Day 2 (no ADRs-as-done, no Consensus, no five-file package, no Parquet).
- Not "install Claude."
- Not a Second Brain with nothing ingested.
- Not OntoLayer as a slide — the MCP question has to land.
- Not Pass 2–4.
- Not a repair of `173.44`.

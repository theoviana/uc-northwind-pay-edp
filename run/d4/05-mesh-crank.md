# 05 · Crank one Type 01 leaf

- Slide: Execute 05–06 (Hands-On **slice c · loop**) — tile 05
- Slice: **C · Loop**
- Who: instructor cranks **one** signed Type 01 leaf in public, then seats
- Next: [`06-packet.md`](06-packet.md) on the **same board**

Task-Mesh Show already ran. Mesh **runs** the leaf. It does not author it. Watch the **eval**, not the keystrokes. Bind still on. This is the trail they just walked — not a remaining-type leaf.

Skip this board if generate has no evals, or Gold is missing.

## Prompt (verbatim)

```text
Pass 8 Loop on NorthWind Pay. Crank exactly one signed Type 01 leaf first.
Do not pick a remaining-type leaf on this tile. The trail is Type 01.

Rules:
- Bind still on. Frozen: legacy/, contracts/, gen/, infra/.
- Unsigned leaves stay dark.
- Eval is the judge. Chat is not.
- Cross-family engines only if cvg doctor is green.
- Record a packet: leaf id, attempt, eval command, exit, classification or skip, paths under evidence/ (terminal, not Git).

Do not settle if the packet is missing.
Do not rewrite golden_match.py.
Do not start Type 05 on this tile.
Tonight the Type 01 packet is already at evidence/loop/T-20260825-type-01-landing-parser.json — look it up; do not invent a second crank.
```

## Proof

One **Type 01** leaf attempted. Eval ran. A **packet** exists under `evidence/` (or the fail is named). Linear did not move without it.

## If fail

Picking Type 02–05 first → stop; that is not the trail. Eval red → retry or stop. Do not “fix expected.” Frozen write → fail closed. No packet → **do not settle**.

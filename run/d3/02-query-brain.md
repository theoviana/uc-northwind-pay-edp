# 02 · Query Second Brain

- Slide: Execute 02–04 (Hands-On **slice a · query** · chip `run/d3/` · **02–04**)
- Slice: **A · Query**
- Who: every seat, **their** notebook from Day 1. Do not rebuild
- Next: [`03-query-graph.md`](03-query-graph.md) on the **same board**

Same nine sources. No tenth file. Paste one, wait, then the next.

## Prompt (verbatim)

**J1**

```text
From the sources in this notebook only:
Does the new plant re-parse raw bytes in the lakehouse, or does something only register landing?
If dlt (or a loader) computes a net, is that in these sources? Cite the page.
If it is not in the sources, say you do not have it.
```

**J2**

```text
From the sources only:
Where does privacy die for Type 01 (PAN, CPF)? At parse, or in Gold?
Cite the page. Do not invent a dbt model that tokenizes.
```

**J3**

```text
What is Bronze vs Silver vs Gold in these sources, if they use those nouns?
If they only sketch a second reader / medallion in a meeting, say that is mail, not a grain.
Cite the page.
```

**J4**

```text
Source lie: trailer 173.44 vs rows 173.45.
Must Gold “fix” the trailer so it matches? What must both plants do?
Cite Marina if she is in the sources.
```

**J5**

```text
Do these sources contain a DuckDB, dlt, or dbt decision? If I need plans/modern.md or an ADR to answer, say you do not have it.
```

## Proof

J2: privacy dies at **sanitize / parser**, not Gold. J4: keep **173.44**; refuse; no repair. J5 does **not** invent a stack. Notebook still **nine** sources. No git writes.

## If fail

No Google / empty notebook → pair. `spec/` is on disk. Do not dump Java into NotebookLM. Do not add a tenth pack.

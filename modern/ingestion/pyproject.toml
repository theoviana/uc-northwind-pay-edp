[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "northwind-pay"
version = "1.0.0"
description = "Modern NorthWind Pay Type 01 ingestion: parse, validate, publish landing Parquet."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

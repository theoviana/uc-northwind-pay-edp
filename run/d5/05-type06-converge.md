# 05 · Type 06 — Converge Engine e2e

- Slide: Execute 05 (Hands-On **slice e · factory** · chip `run/d5/` · **05**)
- Slice: **E · Factory**
- Who: instructor binds, **leaves the table**, then the room looks up
- Next: [`06-flywheel.md`](06-flywheel.md)

This is the Night. Sealed Type `06` into `spec/` (or the share). **No preamble about the miss.** Brain packs cover `01`–`05` only — Type `06` is a **new** source, not in the zip. Do not search git for a prior modern implementation.

Same three seams. Type `06` is a new numbered package, not a new estate. Same modern shape: five-file → landing Parquet → dlt register-only → Gold → golden-match. Consensus **before** the loop. Bind still fences `legacy/` `contracts/` `gen/` `infra/`.

When golden-match returns a difference: **do not speak the answer.** Ask the two questions out loud. If modern matches the contract and legacy does not: `CONFIRMED_LEGACY_DEFECT`. Write it down. Stall. Do not patch Java.

## Prompt (verbatim)

```text
You are Pass 0–8 on a sealed Type 06. Dark Factory seat.

Do not search the repository for Type 06 before the drop lands in spec/.
Do not recut docs/consensus.md or docs/consensus-lakehouse.md.
Do not rewrite validation/golden-match/golden_match.py.
Do not edit legacy/, contracts/, gen/, or infra/ to go green.
Do not dump Types 02–05 modern packages on this tile.

1. Receive the Type 06 inbound kit into spec/ (messy on purpose). contracts/ is the judge.
2. Capture → Intent → Structure → Decompose. Same seams: ingest-landing, dlt-gold, orchestrate-serve.
3. Consensus. Owner signs. No sign → no loop.
4. Task-Spec leaves with evals. signed_off starts false. Evals include classification, not only "it loaded."
5. Bind. Then leave. Mesh runs. Watch the eval, not the keystrokes.
6. Golden-match: two questions, never netted. One code. Packet under evidence/ (terminal, not Git).
7. If modern matches the contract and legacy does not: CONFIRMED_LEGACY_DEFECT. Stall the type. Do not patch.
8. If modern is also wrong: MODERN_DEFECT — fix the new plant, re-run. Still classify legacy if that fact is true.

Linear moves when the packet exists. Chat is not a settle.
```

## Proof

Type `06` inbound arrived **tonight**. Consensus signed before the loop. Packet on disk. Classification named. Frozen trees untouched. Type stalled, not “fixed in place.”

## If fail

Kit not in the room → **dark this board**. Stage and 01–04 still count. Searching git for 06 → stop. Patching Java → restore. No packet → do not settle. No sign → do not loop.

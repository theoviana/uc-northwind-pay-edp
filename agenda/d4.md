# Day 4 — Orchestrator

**Seat:** Orchestrator.  
**Converge:** SeamWise **again** (remaining swimlanes + legs). Task-Spec **generates remaining SWE + DE leaves**. Pass **6–8** (Register / Bind / Loop). **Linear** is the board. Type `05` unattended.  
**This file** is the brief for the Day 4 PPT: generate the backlog, crank it in the loop, unattended Type `05`, small `HALF_UP` pill.  
Keep it short on stage. Build slides from the chapter list. Do not invent extra story. Staff clock still a stub: [`run/d4.md`](../run/d4.md).

Public gate: *the loop assumes; it validates and corrects itself.*  
Rings tonight: **loop + eval.** Full loop.

**One Night.** There is no morning and no afternoon.

---

## Story this week (what already happened)

Tuesday + Wednesday were **steel threads on Type 01 only** — two seats, two lanes, one type:

| Night | Lane | Closed |
|---|---|---|
| **2** Translator | Seam 1 · ingest → landing | Design signed. Parser leaf. Emit if Parquet was missing. |
| **3** Constructor | Seam 2 · dlt → Gold | Lakehouse facts signed. Type 01 landing → Gold + golden-match. Mesh **seed**. |

Types `02`–`04` were **parked**, not tasked. Type `05` was a subject line only. `cvg/swimlanes/` may still be missing (`cvg init` never ran Tuesday) — host pins Task-Spec 3.8.x and inits tonight if the referee must see a workspace. **Do not copy** another project’s lane names (`assurance` / `foundation` / `models`). This plant’s folders are **ingest-landing**, **dlt-gold**, **orchestrate-serve**.

Do not recut Day 2 ingest Consensus or Day 3 lakehouse sign.

---

## Night 4 agenda (the projector)

Tonight we **generate the rest**, then **crank it in front of the room**. You teach Mesh + Pass 6–8 over live evals. Different engines if `cvg doctor` is green. Linear holds the queue.

- [ ] **End-to-End**
  1. *Recap* — Type 01 vertical is Gold. Parser + landing + dlt + B/S/G + golden-match. `02`–`04` parked. Keep 173.44. Tight.
  2. *The Orchestrator* — the seat that **leaves**. If the plant needs you at the keyboard, it is not orchestrated.
  3. *Why a loop* — Dagster is lineage, retries, evidence. It does not parse. Direct and orchestrated must hash the same Gold.
  4. *SeamWise again* — remaining **swimlanes** + **legs**: Type `02`–`04` ingest (SWE) and lakehouse (DE), Type `05`, orchestrate + serve. Not a fourth estate. Not a recut of Type 01.
  5. *Task-Spec generate* — author **all remaining SWE + DE leaves** with evals. Empty packages forbidden. `signed_off` starts false.
  6. *Register + Linear* — Pass 6 so the mesh can see the queue. Project signed leaves onto **Linear**. Move them as they settle.
  7. *Task-Mesh + Bind + Loop (6–8)* — crank the queue. Cross-family engines if doctor allows. You watch the **eval**, not the keystrokes. Frozen folders still fenced.
  8. *Type 05 unattended* — hand the inbound pack. Leave the table. `DF-SOURCE-005` = `CONFIRMED_SOURCE_DEFECT`. Then `rounding-half-up`: `HALF_EVEN` is `MODERN_DEFECT`. Fix the plant, not `expected/`.
  9. *Research* — factory / flywheel. Do not search Type `06`.

Show, then Hands-On. Do not flip a slide per prompt. Recap is **one act**. The generate is **one act**, then the loop is the Floor.

**Swing:** Recap Type 01 Gold → Orchestrator role → loop is not the parser → SeamWise again → Task-Spec generate remaining SWE + DE → Linear + Register → Mesh + Pass 6–8 crank → Type `05` unattended + `HALF_UP` → Debrief.

---

## Intent

Type `01` should already be a Gold vertical. Types `02`–`04` are **parked**, not tasked. Tonight the room runs SeamWise again (remaining **swimlanes** + **legs**), asks Task-Spec to **generate all remaining software-engineering and data-engineering leaves**, projects the queue onto **Linear**, cranks it in Mesh + Pass 6–8 (cross-family engines if doctor is green) while you teach, then lets Type `05` hit the loop unattended. They leave able to:

1. Model the deterministic pieces as Dagster assets. Parsing does not move into the orchestrator.
2. Prove direct execution and orchestrated execution produce the same result.
3. Bind Type `05` and walk away. The eval is the judge.
4. See the small red pill: `HALF_UP` vs Python `HALF_EVEN` vs ops "normal rounding."
5. Classify a wrong default as `MODERN_DEFECT`. Do not change `expected/`. Do not break Java.

They do not open Type `06`. They do not plant a Java bug. They do not require cloud, CI, or Terraform. Serving, if it happens, is read-only approved Gold only.

---

## Stage — chapters for the PPT

Use this order. One idea per slide.

### 1. Orchestration is lineage, retries, evidence

- Dagster senses, depends, retries, partitions, backfills, checks.
- It does not parse overpunch. It does not round a fee.
- Direct and orchestrated must hash to the same Gold.

### 2. The orchestrator

- The seat that leaves. If the plant needs you at the keyboard, it is not orchestrated.
- Bind + Loop after a **second** SeamWise + Task-Spec pass for remaining SWE + DE. Do not recut Day 2 ingest or Day 3 lakehouse signs. Type `05` inbound may force a thin parked ADR.

### 3. Type `05` is a real type, then a pill

- Transport: quote-aware semicolon CSV, NFC, decimal comma.
- Source lie: assessed `0.99` → `1.00` → `SOURCE_CONTROL_ASSESSED_FEE_MISMATCH` (`DF-SOURCE-005`). Same movie as the Type `01` lie.
- Small pill: `rounding-half-up` is `0.04` on `3.50` under **`HALF_UP`**.

### 4. The small red pill (draw the trap)

| Trusted thing | What it does on the half |
|---|---|
| Python default | `HALF_EVEN` |
| Estate / mail language | "normal rounding" |
| Contract + Java on this type | `HALF_UP` |

An agent that copies the default will look structurally green and be one cent wrong. Golden-match names it `MODERN_DEFECT`.

This is **not** a planted Java bug. Java is specified `HALF_UP`. We do not break the frozen plant to teach the lesson.

### 5. Converge tonight

```text
SeamWise again  → remaining types (02–04) + orchestrate/serve lane
Task-Spec       → generate remaining SWE + DE leaves (ingest + lakehouse + Type 05 + Dagster)
6 Register      → mesh can see the queue
7 Bind          → same frozen fence; Type 05 + orchestrator paths
8 Loop          → crank the queue. You watch the eval, not the keystrokes.
                 Type 05 unattended last.
```

Do not recut the estate. Pass 5 tonight is **generate remaining leaves**, not a second Type 01 design.

### 6. What Friday still is

- A **new** type, sealed, not in `spec/` tonight.
- A **different** pill. Do not preview the miss. The week table already named the class: `CONFIRMED_LEGACY_DEFECT`.
- Homework is sleep and a healthy runtime. Not a sneak peek.

---

## Craft — kits on this Night

| Kit | Job tonight |
|---|---|
| **Converge** | Pass 6–8. Coordinator still writes no product code. `cvg init` if no workspace. |
| **SeamWise** | **Second** pass: remaining type lanes + orchestrate/serve. Do not add a fourth estate. |
| **Task-Spec** | Generate remaining SWE + DE. Type `05` evals include `rounding-half-up` and `DF-SOURCE-005`. |
| **Linear** | Board. Projection of signed leaves. Not the judge. |
| **Brief-Spec** | Implement, then a decide hour on the pill. |

### Agent rules tonight

| Rule | Why |
|---|---|
| Unattended means the eval runs without a narrator | That is Friday's rehearsal |
| Do not "fix" expected to match `HALF_EVEN` | That is the pill |
| Do not serve unresolved Gold | FastAPI is read-only approved snapshots |
| Do not author Type `06` | Sealed |

---

## Floor — live work (instructor + tables)

1. Host: pin Task-Spec 3.8.x if needed. `cvg init`. Project **this** plant’s seams into `cvg/swimlanes/` (ingest-landing, dlt-gold, orchestrate-serve). Do not copy foreign lane names.
2. SeamWise + Task-Spec: generate remaining SWE + DE leaves (`02`–`04` ingest + lakehouse, Type `05`, Dagster). Linear: the queue is visible.
3. Crank that queue in the loop (Mesh + Pass 6–8). Teach over live evals. Cross-family if doctor is green.
4. Show Type `01` (and any closed `02`–`04`) under Dagster. Same hash as direct.
5. Bind Type `05`. Hand the inbound pack. Leave the table.
6. When the loop returns: walk `valid-minimal`, malformed, `DF-SOURCE-005` (`CONFIRMED_SOURCE_DEFECT`).
7. Then open `rounding-half-up`. If Gold is `0.03` or `HALF_EVEN`, it is `MODERN_DEFECT`. Fix the plant.
8. Re-run until the assessed cent is the contract's cent. Java still MATCHED. `expected/` untouched.
9. Optional stretch: one read-only FastAPI endpoint on approved Gold. No raw, no PII, no unresolved.

Do not walk Type `06`. Do not "just change the Java rounding to make a better demo."

---

## Debrief

- You now have a plant that can take a type you did not pair-program.
- The small pill is the preview: something you already trusted can be wrong.
- Friday the trusted thing may be the **legacy plant**. Classify. Do not patch.

---

## Homework

1. Runtime healthy. Types `01`–`05` that you claim closed have evidence packets.
2. Do not search the repo for Type `06`. It is not there.
3. Sleep. The factory run starts on a sealed drop, not on leftover Type `05` work.

---

## Research

Query tonight for Day 5. Same notebook as Day 1
([`brain/notebooklm/`](../brain/notebooklm/README.md) — Type `05` HALF_UP is pack 07). Do not rebuild it.

1. **Second Brain** — "What is a factory, and what is the flywheel?" The
   process, not a detector folder.
2. Do **not** search the repo for Type 06. It is not in the notebook. It is not in `spec/`.
3. Restate the two lies you already met. What might the third be?

---

## Exit criteria

- [ ] Remaining SWE + DE leaves authored (`02`–`04`, Type `05`, orchestrate). Linear shows the queue.
- [ ] Mesh + Pass 6–8 cranked that queue (eval is the judge).
- [ ] Dagster direct/orchestrated equivalence on the Type 01 steel thread.
- [ ] Type `05` ran unattended through landing → Gold → golden-match.
- [ ] `DF-SOURCE-005` is `CONFIRMED_SOURCE_DEFECT`.
- [ ] `rounding-half-up` is `HALF_UP`. A `HALF_EVEN` default was classified `MODERN_DEFECT` and fixed in the plant, not in `expected/`.
- [ ] Frozen `legacy/` untouched. Type `06` still absent.
- [ ] Serving, if present, is read-only approved Gold only.

## Day 4 is not

- Not Day 3 (do not redesign grains to dodge the pill).
- Not Day 5 (no sealed kit, no `CONFIRMED_LEGACY_DEFECT` hunt on Types `01`–`05`).
- Not a Java sabotage.
- Not CI, cloud, or Terraform.
- Not "the agent said it rounded normally, so we shipped it."

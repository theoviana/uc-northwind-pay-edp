# 08 · Pass 4 — Lakehouse Consensus

- Slide: Execute 09 (Hands-On **slice b · barrier**) — tile 08
- Slice: **B · Barrier**
- Who: a **different** voice, then the owner
- Next: Task-Spec Show, **pause** for the Task-Spec kit, return to Execute 10, then [`09-taskspec.md`](09-taskspec.md)

If they will not sign, **skip 09–12**. Do not overwrite [`docs/consensus.md`](../../docs/consensus.md) (ingest → landing stays canonical). Do not paste leaves from inside the Task-Spec kit.

## Do

1. Adversary pass on **lakehouse facts** (0007+ and seam 2 legs). Every objection **FIXED** or **ACCEPTED**.
2. Walk one contradiction if needed: dlt-as-parser vs register-only; Gold copied from Postgres vs observation. Keep **173.44**.
3. Owner signs. Record: `docs/consensus-lakehouse.md` (who, date, FIXED / ACCEPTED). Ingest sign is **not** recut.

```bash
cvg consensus --sign --json
```

If `cvg` is missing **or errors**, a dated signature in `docs/consensus-lakehouse.md` still counts. If `cvg` wrote elsewhere, copy it here. Do not debug the CLI.

## Proof

`docs/consensus-lakehouse.md` exists. Every objection FIXED or ACCEPTED. The room can point at **this** sign **and** Tuesday’s ingest sign. `docs/consensus.md` verdict unchanged.

## If fail

Unsigned → **do not run 09–12**. Stay on design. Do not write Gold and sign later. Do not “update” ingest Consensus to smuggle a lakehouse.
